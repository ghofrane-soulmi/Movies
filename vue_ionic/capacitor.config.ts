import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vue_ionic',
  appName: 'vue_ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
